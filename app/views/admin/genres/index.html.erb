<p>Admin::Genres#index</p>
<h2>ジャンル一覧・追加</h2>
<%= form_with model: [:admin, @new_genre] do |f| %>
  <%= f.label :ジャンル名 %>
  <%= f.text_field :name, autofocus: true, placeholder: 'ジャンル名' %>
  <%= f.submit '新規登録', class: "btn btn-success" %>
<% end %>